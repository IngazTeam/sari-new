# ساري — سجل التحقق والإصلاح بتاريخ 10 سبتمبر 2026

هذا السجل هو تحديث الحالة المحلية بعد تقرير أغسطس، وليس شهادة نشر أو اختراق مستقل.
المرجع عند بدء الجولة: `df78b29`؛ فحوص المرحلة الأولى نُفذت على هذا المرجع مع التعديلات المبينة أدناه.

## المرحلة الأولى — قابلية تكرار بوابات الإصدار والحزمة

### العيوب المثبتة والإصلاح

1. حارس ZahyPi يقارن مسارات Windows بمسارات POSIX؛ لذلك كان يسجل الملف القديم كمسار حي ويفشل allowlist المزودين. وُحدت المسارات قبل المقارنة مع اختباري Windows/POSIX، دون توسيع قائمة السماح أو إخفاء call-sites.
2. بناء الحزمة يفشل بـ`spawn zip ENOENT` على Windows. استُبدلت أدوات `zip/unzip` بـJSZip مثبتة صراحة في devDependencies والقفل، مع وقت وصلاحيات وترتيب ثابتة.
3. منشئ الحزمة كان يحذف الملف المستهدف قبل نجاح البناء. صار يتحقق في الذاكرة أولًا، ويرفض استبدال تسليم مختلف، ويسمح بإعادة البناء المطابق فقط. أضيف منع تجاوز المسارات، وتكرار الأسماء/aliases، والتواريخ غير الصحيحة، والأسرار والحجم الزائد، وفحص جميع بصمات Manifest وCRC.
4. اختبارات الموصل الجديدة لم تكن كلها في CI أو بوابة النشر. أضيفت `test:zahypi` لكليهما، مع اختبار يمنع حذفها؛ تشغيل النشر يحجب DATABASE_URL وملف أسرار التشغيل عن الاختبارات.
5. فشل حارسا النشر وملكية هاتف واتساب بسبب CRLF؛ وُحدت قراءة النص للاختبارات دون تغيير assertions الأمنية، وثُبتت LF لسكربتات Bash عبر `.gitattributes`.

### الأدلة المحلية

بيئة التنفيذ: Windows، Node `24.19.0`، pnpm `10.4.1`، Vitest `4.1.10`.
نسخة الإنتاج المطلوبة في `.node-version` هي `22.17.1`؛ نتائج Windows لا تحل محل CI على النسخة المثبتة.

| الفحص | النتيجة |
|---|---|
| `pnpm test:zahypi` | 162 نجاحًا في 15 ملفًا |
| `pnpm test:release` — pretest التلقائي | 222 نجاحًا وskip واحد في 18 ملفًا |
| `pnpm test:release` — المجموعة الرئيسية | 887 نجاحًا في 72 ملفًا |
| `tsc --noEmit` | ناجح بلا أخطاء |
| `pnpm build` وميزانية الحزمة | ناجح؛ main = 570,834 bytes، gzip = 170,944 bytes |
| `drizzle-kit check` | ناجح؛ فحص تاريخ المخطط فقط بعنوان وهمي، دون ترحيل قاعدة |
| تدقيق اللغات | العربية المعلنة 99.6%؛ اللغات غير المكتملة لم تُفعّل |
| `bash -n` لسكربتات النشر الثلاثة عبر Git Bash | ناجح؛ لم تُنفذ عملية نشر |

الـskip هو فحص Bash الآلي المستثنى على Windows، وقد نُفذ فحص التركيب منفصلًا أعلاه.
الأعداد بين البوابات متداخلة؛ لا تجمع كعدد اختبارات فريدة. استجابات المزود والموصل mocked في الاختبارات، وليست إثبات دفع/رسائل أو تفعيل حي.

## المرحلة الثانية — إغلاق فجوة التحقق من العقود وهوية التفعيل

- كان عميل `/v1/jobs` يقبل `applicationResponse` دون التحقق من بقية مخطط المهمة. أصبح يتحقق من المدخلات والمخرجات باستخدام JSON Schema الكتالوج عبر Zod، ويرفض الحقول الزائدة والناقصة والقيم المتجاوزة دون إظهار محتوى المزود في الخطأ.
- رُبط `structured_output.traceId` بالتتبع المتوقع، مع اختبارات تثبت رفض النتيجة المخالفة دون إعادة طلب النموذج.
- كان موصل التفعيل يتحقق من شكل إيصال التنفيذ فقط، دون ربطه بالمشروع/tenant/المهمة/trace المطلوبة. أصبح يرفض أي اختلاف ويشترط مخرجًا مطابقًا للمخطط والتتبع.
- أصبح طلب التفعيل يستخدم غلاف `input.messages` و`business_input` نفسه المستخدم في التشغيل، بمدخلات مصطنعة فقط.
- لا تتغير أسماء المهام أو schemas المنشورة بهذه المرحلة؛ تتشدد شروط قبولها فقط. يلزم التحقق من استجابة ZahyPi الفعلية قبل النشر؛ الغلاف القديم الناقص سيُرفض عمدًا.
- اختبارات النطاق: **150/150** في أربعة ملفات. بوابة ZahyPi: **52/52** لاختبارات المخططات و**173/173** للربط والموصل والحزمة. تشمل كل المهام الخمسين، منها مهمتان planned، ولا تثبت جودة رد نموذج حي.

## المرحلة الثالثة — مزامنة التسليم مع التنفيذ

- المنشئ الحالي يولّد 50 مهمة من مصدر الكود، منها 48 existing ومهمتان planned، بدل كتالوج أغسطس اليدوي ذي 36 مهمة.
- أضيفت بصمات مصادر الكود ومراجع الأسطر، والمسار الحالي `/v1/jobs` منفصل عن المتخصص المقترح، وحدود الصوت/embeddings وlegacy والعقد المتوافق.
- لكل مهمة سبع حالات مصطنعة مختلفة فعلًا في المدخلات، تشمل العربية والإنجليزية والطول والحقن والمعلومات الناقصة، مع خمس حالات رفض. هذه مرشحات لفحص schema وليست نتائج تقييم نموذج حي؛ المراجعة الدلالية ما زالت شرطًا.
- `--verify` يجمع ملفات البوابات دون تكرار، يشغّلها، ويضمّن ملخصًا آليًا آمنًا مربوطًا بـSHA. لا تُكتب PASS عند عدم التشغيل. CLI يرفض SHA قديمًا أو تغييرات مصدر غير مكمّتة، ويعيد التأكد بعد الاختبارات.
- حزمة 26 أغسطس والسكربتان اليدويتان تبقى كما هي للأرشفة؛ لا تُسلم بوصفها نسخة المصدر الحالي.
- المتبقي في الحزمة نفسها: استكمال واعتماد خريطة UI/permission/storage لكل caller، والمخططات الدلالية المتخصصة بدل الاكتفاء بغلاف التوافق، وموافقات المالك. لذلك ليست READY_FOR_PRODUCTION.

### التحقق البعيد المقروء في 10 سبتمبر

تمت قراءة [CI رقم 35](https://github.com/IngazTeam/sari-new/actions/runs/33299702363) من GitHub مباشرة:
المرجع `df78b2970d540156f69cde6a43a51b7875f5fb52` بتاريخ 30 أغسطس؛ **Quality/build وDatabase integration كلاهما success**.
هذا يثبت CI للمرجع السابق فقط، ولا يشمل كوميتات هذه الجولة المحلية. لم تُرفع أو تُنشر تلقائيًا.

البيئة المنشورة: GET `/health` = 200 و`healthy`؛ GET `/ready` = 200 و`ready` مع `database=connected` و`schema=current`. لم يُستخدم حساب أو مزود مدفوع ولم يحدث تغيير في بيانات العملاء. لا تثبت الصحة العامة SHA النشر أو رحلة الوظائف الكاملة.

## شروط الإغلاق التشغيلي التي لم تثبت في هذه الجولة

- SHA المنشور وCI البعيد وMySQL integration على Node المثبت، وتطبيق الترحيلات الآمن مع backup/restore.
- رحلة ZahyPi وTap وWhatsApp/الصوت وبيان الحية، بما فيها tenant A/B وwebhook replay.
- رقم ساري المخصص: تنفيذ/اعتماد قناة المنصة وموافقة المستلم والقالب، ثم اختبار تسليم حي.
- تدوير الاعتمادات التاريخية، وفحص حمل واستعادة، وبنتست مستقل.
- موافقات المالك وحدود التكلفة والاحتفاظ وShadow/Canary، وإثبات تجاري بقياسات فعلية وموافقات العملاء.

لا توجد نتيجة تدعم وصف المشروع بأنه 10/10 تشغيليًا أو رفع هذه التعديلات إلى الإنتاج تلقائيًا.
