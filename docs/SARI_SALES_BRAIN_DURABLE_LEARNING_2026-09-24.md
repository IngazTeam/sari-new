# سجل تحليل التعلّم الدائم — 24 سبتمبر 2026

استكمال بعد `6e29230e` لخطة مخ المبيعات. كان منع التكرار داخل عملية Node فقط: عاملان قد يستدعيان النموذج للعينة نفسها، حتى إن منع SQL تكرار حفظها. الإصلاح يضع قرار الاتصال في قاعدة البيانات، ويفصل استعادة الحفظ عن إعادة الاتصال بالمزوّد. لا يثبت جودة الإقناع أو تحسن التحويل، ولا يمنح اعتمادًا تلقائيًا للمقترحات.

## السلوك المنفّذ

1. خانة واحدة لكل نشاط في `ai_learning_analysis_jobs`، مرتبطة بالنشاط بمفتاح أساسي وFK. تحفظ معرّفات المصادر وبصمتها ورمز الحجز؛ لا تحفظ نسخة أخرى من رسائل العملاء أو البرومبت.
2. الحجز `reserved` مدته 60 ثانية بساعة MySQL، ويمكن استعادته بعد انتهائه مع رمز جديد يبطل العامل السابق. أي عينة أخرى للنشاط تُحجب أثناء العمل القائم، بما فيها العينات المتداخلة.
3. يُثبت `dispatched` بمعاملة ناجحة قبل دخول `callGPT4`. لا يحجز SQL أثناء انتظار الشبكة. يستمر اختيار OpenAI/ZahyPi المركزي وسقف المنصة اليومي 100 دولار؛ `noRetry:true` يمنع إعادة المحاولة الداخلية لهذا التحليل.
4. انقطاع الشبكة أو انهيار العامل بعد تثبيت الإرسال لا يفتح إرسالًا ثانيًا. فقد إقرار تثبيت الإرسال قد يترك مهمة `dispatched` لم تُرسل فعليًا؛ تُحجب أيضًا، لأن الإقرار وحده لا يثبت ما حدث. لا يوجد ضمان «تنفيذ خارجي مرة واحدة» من المزوّد.
5. أخطاء `AiBudgetError` المحددة `budget_exceeded/price_required/policy_required/identity_required` تقع قبل النقل؛ تدوّر رمز الحجز وتؤجله خمس دقائق. `budget_unavailable/invalid_usage` وغيرها قد تأتي بعد نجاح المزوّد، فتظل مجهولة. لا تُقبل سلسلة نصية أو كائن مزور بوصفه إثبات رفض قبل الإرسال.
6. تُفحص الاستجابة كاملة قبل تخزين JSON صالح وبصمته في `responded`. لا يُخزن النص الحر المرفوض. تشمل الحماية المراجع الوهمية، والنتيجة الناقصة، وتكرار المقترح بعد تطبيع نصه. تبقى المصادر غير مستهلكة عند الرفض.
7. تُستعاد النتيجة المحفوظة قبل فحص عتبة الإشارات الحالية، من مصادرها الأصلية مع إعادة فحص النص والبصمة والملكية. لا تُربط بعيّنة جديدة. تغيّر المصدر أو حذفه يبطلها ويمسح نص الاستجابة عند محاولة الاستعادة.
8. حفظ المقترحات والأدلة وتعليم المصادر وإتمام المهمة ومسح نسخة الاستجابة يحدث في معاملة واحدة. فشل الخطوة الأخيرة يرجع الجميع؛ فقد إقرار commit الناجح لا يكرر الإسقاط أو الإشعارات. وصول رد متأخر من صاحب الطلب المجهول نفسه مقبول إذا بقيت المصادر صحيحة.
9. المقترح المتقاعد لا يعاد فتحه؛ تُرفض النتيجة وتُمسح نسخة الاستجابة. النتيجة الصحيحة بلا نمط تسجل إتمامًا بعدد صفر وجيل NULL. جميع المقترحات الجديدة تبقى `proposed`.

## الاختبارات والنتائج

نجح **6416 اختبارًا فريدًا** دون إخفاق أو تخطي، بإجمالي 8302 تنفيذ يشمل تكرار المجموعات. أضيفت 38 حالة MySQL لهذه الدفعة، ونجح الفحص المركّز السابق في 127 حالة.

| الفحص | النتيجة |
|---|---|
| unit | 1820 ناجحة |
| database | 1835 ناجحة |
| regression | 1038 ناجحة |
| legacy-sales | 229 ناجحة |
| budget | 11 ناجحة |
| security | 3369 ناجحة |
| TypeScript / build / Drizzle | ناجحة؛ الحزمة الرئيسية 102318 بايت gzip |
| الترجمة | 6393 استدعاء / 244 مفتاح ديناميكي؛ لا أخطاء |
| المهاجرات | 101 مهاجرة محلية؛ حفظ المصادر السابقة، وعدم إنشاء مهام رجعية، وإعادة الترحيل بلا تغيير |
| مدخلات الواجهة | 505 ملفًا ثابتًا وCSS مطابق؛ إعادة استخدام 679 حالة Chromium و58 لقطة سابقة |

مصادر الاختبارات النهائية من نسخة مستقلة مطابقة للكوميت؛ تحققت بصمات 375 ملف مصدر. كشف الفحص الأول استخدام spread على Set لا يلائم هدف TypeScript الحالي؛ استُبدل بـArray.from ثم أعيد التحقق على نسخة نهائية مستقلة.

المراجع: [الأدلة](./audits/sales-brain-implementation-2026-09-23/evidence.json)، [مصفوفة القبول](./audits/sales-brain-implementation-2026-09-23/acceptance-matrix.md)، [المهاجرات](./audits/sales-brain-implementation-2026-09-23/durable-learning/migration.json)، [ثبات مدخلات الواجهة](./audits/sales-brain-implementation-2026-09-23/durable-learning/ui-reuse.json). لا تشغيل جديد للمتصفح؛ الدليل السابق يستخدم API fixtures ولا يثبت قبولًا إنتاجيًا أو اختبار iPhone فعليًا.

اختبارات MySQL تشمل التنافس داخل العملية وبين ثلاث عمليات Node مستقلة، انتهاء الحجز، منع رمز قديم أو نشاط آخر، تعليق المهلة، استعادة الرد قبل عتبة الإشارات، حذف المصادر ونقل ملكية المحادثة، فساد JSON وبصمته، التراجع عن فشل إتمام المهمة، وفقد إقرار الإرسال وحفظ الرد والإسقاط. اختبارات المحرك تستبدل المزوّد والإشعارات بمحاكاة؛ لا اتصالات مدفوعة أو رسائل للعملاء.

## الترحيل والتشغيل

- ترحيل `0100_learning_analysis_jobs` إضافي؛ لا يعدل الإشارات أو المقترحات القائمة ولا يجري تحليلًا رجعيًا. يتحقق المسار من الجدول والمفتاح الأساسي قبل العمل، وتتحقق جاهزية الإقلاع من وجوده.
- يجب نشر الترحيل والكود على الويب وكل العمال معًا وإيقاف النسخ القديمة؛ العامل القديم لا يعرف القفل الدائم ويمكنه تجاوز البروتوكول. لم يحصل دفع أو نشر في هذه الدفعة.
- تشخيص محلي/تشغيلي دون قراءة محتوى العملاء: `SELECT merchant_id,state,failure_code,generation,proposal_count,created_at,updated_at FROM ai_learning_analysis_jobs;` لا تحوّل `dispatched/uncertain` إلى حجز جديد لمجرد مرور الوقت. إثبات نتيجة الطلب وتسويتها يحتاج مسار مراجعة تشغيليًا مستقلًا.
- إعادة التحقق: `scripts/testing/run-sales-brain.mjs --with-database --regression --security` مع `SARI_TEST_DATABASE_URL` لقاعدة محلية قابلة للإتلاف؛ المشغّل يحجب الشبكة الخارجية ومفاتيح الإنتاج.

## المتبقي والحدود

- لم تُنفذ واجهة أو API لاعتماد تسوية `dispatched/uncertain`، ولا ربط مباشر بمعرّف طلب المزوّد في دفتر المهمة. تبقى الحالة المجهولة محجوبة، بما في ذلك بعض أخطاء الإعداد التي لا تحمل إثباتًا موثوقًا لعدم الإرسال.
- الاستعادة تعمل عند استدعاء مسار التحليل الحالي، تلقائيًا من الإشارات أو يدويًا من مسار المخ؛ لا عامل دوري مستقل لاستعادة الردود المحفوظة. تعطل الحفظ قبل تخزين الاستجابة قد يفقد النتيجة ويترك الإرسال محجوبًا.
- خانة النشاط تحتفظ بآخر مهمة فقط؛ ليست سجلًا تاريخيًا للتجارب. بعد الإتمام يبقى hash النتيجة وعدد المقترحات والجيل، ويُمسح JSON. الرد المحفوظ المتعطل يبقى حتى الاستعادة أو حذف النشاط؛ لا توجد مدة احتفاظ تلقائية جديدة أو محو شامل خاص بكل عميل في هذه الدفعة. حذف النشاط يحذف الخانة بالـFK.
- الإشعار يأتي بعد commit دون outbox خاص به. انقطاع العملية بعد نجاح commit قد يفقد الإشعار؛ لا يُعاد تلقائيًا لتجنب تكراره. هذا لا يفقد المقترحات المحفوظة.
- تقييم مستقل للإقناع، والتجربة المضبوطة، واعتماد نسخة السياسة والتراجع عنها، وقياس الأثر التجاري ما زالت مفتوحة. لا تفعيل تلقائي ولا ادعاء جاهزية 10/10.

حالة الخطة **4 مكتملة / 57 جزئية / 10 مفتوحة**. تقدمت B016/B042/B043/B063/B064/B065 بأدلة جديدة، وتبقى جزئية.
