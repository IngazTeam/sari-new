# تبسيط إعداد حساب التاجر — 24 سبتمبر 2026

تم دمج تصميم لوحة التاجر الجديدة في `/merchant/setup-wizard` واختصار الرحلة من عشر شاشات إلى أربع:

1. **نشاطك:** نوع النشاط والاسم ورقم التواصل؛ العنوان والوصف وساعات العمل داخل تفاصيل اختيارية.
2. **ما تقدّمه:** إضافة المنتجات والخدمات يدويًا، أو استيراد موقع، أو اختيار قالب من الشاشة نفسها. يمكن المتابعة دون كتالوج.
3. **مساعدك:** النبرة واللغة معًا، ورسالة الترحيب والمعاينة داخل قسم اختياري. ربط Google يبقى متاحًا من التكاملات بعد الإعداد.
4. **المراجعة:** ملخص قابل للتعديل وزر تأكيد واحد. الوصول إلى المراجعة لا يُكمل الحساب ولا يُفعّل واتساب.

## التصميم والسلوك

- استخدام ألوان ومتغيرات `merchant-workspace` نفسها: أخضر داكن، خلفية فاتحة، وبطاقات وحقول واضحة.
- قائمة مراحل على الكمبيوتر، وقائمة قابلة للفتح على التابلت والجوال. اختيارات النبرة واللغة مختصرة على الشاشات الصغيرة.
- الحفظ والاستكشاف يعيدان المستخدم إلى اللوحة بعد نجاح حفظ المسودة. يظهر فشل الحفظ مع إعادة المحاولة.
- تظل مسودات النسخة القديمة متوافقة مع عقد الخادم ذي الخطوات 1–10: المراحل الجديدة تقابل 1–3، 4–6، 7–9، ثم 10. لم يتغير عقد API أو مخطط الإنتاج.
- تُستعاد المسودة الحديثة قبل عرض الحقول. لا تُكتب البيانات المؤقتة القديمة فوقها عند إعادة فتح الصفحة. تُنفّذ طلبات الحفظ بالتسلسل، ويأتي التأكيد بعد آخر حفظ.
- تعديلات النشاط والمساعد والكتالوج تبقى عند الرجوع أو تغيير طريقة الإدخال. معرّفات العناصر الجديدة لا تعتمد على التوقيت بالمللي ثانية.
- تصحيح نصوص حالة الحفظ والنجاح والمراجعة التي كانت تستخدم مفاتيح قديمة في مواضع خاطئة. رسائل أخطاء الخادم الداخلية لا تعرض SQL للمستخدم.
- أزرار اختيار فعلية، أسماء مقروءة لأزرار الحذف، تركيز عنوان المرحلة عند الانتقال، وقيمة تقدم مقروءة للتقنيات المساعدة. المعاينة توضيحية محلية ولا ترسل رسائل للعملاء.

## التحقق

- **43 اختبارًا ناجحًا** في خمس مجموعات: واجهة الإعداد، توافق المراحل القديمة، وإصلاحات وحدود الإعداد الحالية.
- تغطي اختبارات الواجهة المسودة القديمة أثناء إعادة الجلب، استعادة خطوة اللغة السابقة، التأكيد الصريح، فشل الحفظ، ترتيب الطلبات البطيئة، ورسالة الخطأ الآمنة.
- فحص TypeScript والبناء الإنتاجي وحد حجم حزمة الدخول: ناجح.
- فحص الترجمة: 6,347 استدعاء و244 مفتاحًا ديناميكيًا؛ صفر مفاتيح مفقودة أو متغيرات ناقصة أو استدعاءات ديناميكية غير محلولة. لغة الواجهة المعلنة بقيت العربية؛ أضيفت النصوص المقابلة لكتالوج الإنجليزية أيضًا.
- تجربة متصفح محلية فعلية: التحقق من الحقول المطلوبة، إضافة منتج، التنقل إلى الاستيراد والقوالب ثم العودة، اختيار النبرة واللغة، حفظ المسودة والخروج وإعادة الفتح، تعديل النشاط من المراجعة، ثم التأكيد والانتقال إلى اللوحة.
- فحص الكمبيوتر ومقاسات 768 و390 و320 بكسل. عرض المحتوى لم يتجاوز عرض الصفحة في المقاسات المفحوصة، ولم تظهر مفاتيح ترجمة خام في المراجعة.
- بعد التأكيد أثبتت قاعدة الاختبار: `setupCompleted=1`، منتج واحد بسعر `2500` من الوحدات الصغرى (= 25 ريالًا)، والنبرة `professional` واللغة `en`.

## نطاق التشغيل

الاختبار على `127.0.0.1:3017` وقاعدة `sari_ux_test` المعزولة على المنفذ 3317، بحساب تجريبي مخصص. احتاجت قاعدة الاختبار القديمة عمود `bot_settings.auto_discount_revision` من الهجرة الموجودة `0074` قبل اختبار التأكيد؛ أضيف إلى التجهيز المحلي فقط. لم يُعدّل مخطط الإنتاج أو حسابات حقيقية.

لم يُختبر استيراد موقع خارجي أو ربط Google/WhatsApp الحي في هذه الجولة. لم تُشغّل المجموعة الكاملة للمشروع؛ التحقق الآلي موجه لهذا التغيير مع البناء وفحص الأنواع والترجمة. لم يُنشر تحديث الويزرد إلى الإنتاج في هذه المهمة.
