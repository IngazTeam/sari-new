// Legacy import alias. The application route uses the canonical callback page,
// so this file must not retain a second payment-status implementation.
export { default } from './SubscriptionPaymentCallback';
